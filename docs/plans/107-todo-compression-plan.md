# TODO 历史完成项压缩计划

日期：2026-07-06

## 背景

当前 `TODO.md` 已累计大量已完成流水账，文件可读性下降。用户指出前面完成的部分应合并、压缩。

## 目标

- 将 `TODO.md` 恢复为当前任务板，而不是完整审计日志。
- 压缩已完成历史，只保留阶段摘要和追溯入口。
- 保留当前未完成工程化整理事项的细粒度清单。
- 不修改代码、不迁移文件、不删除本地数据、不调用阿里云。

## 压缩原则

- 已完成事项按阶段合并为摘要。
- 历史细节以 `reports/rpd-rotation-detection.md` 和 `docs/plans/` 为追溯入口。
- 当前未完成事项继续保留逐项 checkbox，便于执行和验收。
- 被用户明确跳过的清理执行事项标注为“跳过执行”，不再作为待确认阻塞项。

## 预期改动

- 修改 `reports/rpd-rotation-detection.md`，记录 TODO 压缩需求和原则。
- 重写 `TODO.md`：
  - 保留“当前审核点”。
  - 新增“已完成历史摘要”。
  - 保留“当前执行队列”和“后续业务待办”。
  - 删除大段已完成逐条流水账。

## 验收

- `TODO.md` 中当前未完成任务仍完整覆盖工程化阶段 1 到阶段 6。
- `TODO.md` 不再包含数百行已完成历史流水账。
- Git 仍保持单提交历史。
