# Configuration Templates

This directory contains public configuration templates only.

Use `blueprint-normalizer.example.toml` as the starting point for a real local config:

```text
blueprint-normalizer.toml
```

The real config file is intentionally ignored by Git. It may contain local paths and API credentials, and must not be committed.
